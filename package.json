{
  "name": "professional-readme-generator",
  "version": "1.0.0",
  "description": "Dynamically generates a professional README.md file from a user's input using the CLI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Agia",
  "license": "MIT",
  "dependencies": {
    "inquirer": "^8.2.5"
  }
}
